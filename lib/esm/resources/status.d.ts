export default status;
/**
 * `POST /status`
 * Returns the service status and environment.
 *
 * @returns {Promise} Resolves to the result of
 *                    the request or to an error.
 */
declare function status(opts: any): Promise<any>;
