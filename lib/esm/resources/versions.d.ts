export default versions;
/**
 * `GET /versions`
 * Returns the available versions from API.
 *
 * @return {Promise} Resolves to the available versions
 *                   from API.
 */
declare function versions(opts: any): Promise<any>;
